#include <string.h>
#include <stdio.h>
void main()
{
    char a;
    int b;
    float c;
    double d;
    printf("size of char: %lu\n size of int %lu \n size of float %lu \n size of double %lu\n", sizeof(a), sizeof(b), sizeof(c), sizeof(d));
}